#!/bin/bash

set -ex

namespace=${DOCKERHUB_NAMESPACE:-seadas}
image=${IMAGE_NAME:-find-make-matchup-harmony}
tag=${1:-latest}

remote="${namespace}/${image}:${tag}"

docker tag "${image}:${tag}" "${remote}"
docker push "${remote}"
